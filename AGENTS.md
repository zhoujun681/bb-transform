@C:\Users\Administrator\.codex\RTK.md

# Project Versioning

- The visible app version has one source of truth: the `data-version` attribute on `.app-version` in `index.html`.
- Increment the patch version for every repository change unless the user explicitly requests a different versioning scheme.
